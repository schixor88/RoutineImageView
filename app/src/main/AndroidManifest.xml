<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kushagra.routine">

    <application
        android:allowBackup="true"
        android:icon="@drawable/examlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/examlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pageStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pagePreMenu" />
        <activity android:name=".pageBoardMenu" />
        <activity android:name=".pageCmpBoard" />
        <activity android:name=".page1Pre" />
        <activity android:name=".page6Pre"></activity>
    </application>

</manifest>